package cc.yiueil.handler;

import org.springframework.stereotype.Component;

@Component
public class CustomExceptionHandler {
    // 这个类完全是空的，它的唯一目的就是作为一个“占位符”存在。
    // 它不需要继承任何类，也不需要任何方法。主要的作用就是让框架的 CustomExceptionHandler 不进行自动注入
}